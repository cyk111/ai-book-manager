---
description: 项目约束 - 架构、配置、日志、数据库、版本控制、性能、API 设计
globs: **/*
alwaysApply: true
---

# 项目约束

## 架构
- 关注点分离：业务逻辑禁止放在 UI 代码、API handler 或数据库访问层中。这些层是外部世界和核心领域之间的适配器。
- 依赖方向：依赖由外向内流动。领域/业务逻辑不依赖任何外部模块。基础设施依赖领域，不可反向。UI 依赖应用层，不直接依赖基础设施。
- 禁止循环依赖：依赖图必须是有向无环图（DAG）。A 导入 B，则 B 不能导入 A（直接或传递）。
- 副作用在边界：所有 I/O、网络调用、系统交互发生在最外层。核心逻辑纯函数化，无需 mock 即可测试。

## 配置
- 环境变量驱动：所有环境相关值（URL、凭证、特性开关、超时）来自环境变量或类型化配置系统——禁止硬编码。
- 禁止代码中存放密钥：API 密钥、密码、token、连接字符串禁止出现在源码中。使用 secrets manager 或环境变量。
- 合理默认值 + 显式覆盖：每个配置项都有开发环境合理默认值，生产环境显式覆盖。

## 日志
- 结构化日志：日志为机器可解析格式（JSON）。每条日志至少包含：时间戳、级别、消息、关联 ID（trace ID）。
- 日志级别约定：
  - ERROR：系统异常，需要人工立即关注
  - WARN：非预期情况但系统已恢复或降级
  - INFO：关键业务事件（注册、下单、支付）
  - DEBUG：开发者诊断信息，开发环境开启，生产环境关闭
- 每条日志可操作：陌生的 on-call 工程师仅凭日志就能理解发生了什么以及该怎么办。

## 数据库
- 迁移可回滚：每个 schema 迁移有对应的 down/rollback。不可回滚的迁移需显式标记和人工审批。
- 敏感数据不入日志：密码、token、PII 禁止出现在日志、错误消息、堆栈跟踪中。
- 查询必须有 LIMIT：无 LIMIT 的查询视为潜在缺陷。默认分页大小 ≤ 100 行。

## 版本控制
- Conventional Commits：feat: / fix: / chore: / docs: / test: / refactor: / perf:。主题行祈使句，≤ 72 字符。
- 一次提交 = 一个逻辑变更：不把重构和功能混在同一个提交中，不把 bug 修复和功能混在一起。
- PR 要求：每个 PR 描述包含：改了什么、为什么、怎么测试、UI/行为变更时附截图/日志。

## 性能与韧性
- 外部调用必须有超时：每个 HTTP 请求、DB 查询、RPC 调用有显式超时。无超时 = 系统可能无限挂起。
- 退避重试：瞬时故障自动重试，指数退避 + 抖动。设置最大重试次数。
- 禁止 N+1 查询：获取关联数据时用批量查询或 JOIN。循环体内有数据库查询几乎一定是错误的。
- 明确故障策略：每个特性在依赖故障时是降级运行（fail open——如推荐列表为空）还是阻断操作（fail closed——如支付必须成功），需明确决策并记录。

## API 设计
- 从第一版开始版本化：API URL 含版本前缀（/v1/、/v2/）。修改响应字段 = 新版本。
- 写操作幂等性：PUT 和 DELETE 幂等。POST 创建资源使用幂等键保证重试安全。
- 错误响应一致：所有错误遵循 `{ "error": { "code": "...", "message": "...", "details": {} } }` 结构。
- 列表接口分页：禁止无界返回。每个列表响应包含分页元数据。
