---
description: 测试要求 - 覆盖率、测试质量、测试类型、命名规范
globs: **/*.test.*,**/*.spec.*,**/test/**/*,**/tests/**/*
alwaysApply: false
---

# 测试要求

## 核心原则

**测试的真正目的是验证核心流程，避免低级 bug，不是为了刷覆盖率数字。**
- 优先测试核心业务逻辑（scanner、parser、ai-client、queue-service）
- 胶水代码（commands、views、简单 CRUD wrapper）不做无意义的 mock 测试
- 严禁为了覆盖率数字写无效测试

## 覆盖范围
- 每个公开 API 必须有测试用例。公开 API 无测试视为 bug。
- 全局行覆盖率 ≥ 60%，核心模块自然达到 80%+。
- 边界用例必测：空/零/空白输入、边界值（刚好低于和超出限制）、错误路径（依赖失败时的行为）、共享状态的并发访问。

## 测试质量
- 每个测试验证一个逻辑行为。同一逻辑结果的多个断言可以接受；在一个测试中验证无关的事情不行。
- Arrange-Act-Assert 三段式：准备数据 → 执行操作 → 验证结果。不允许断言和准备代码混在一起。
- 测试独立性：测试之间不能有执行顺序依赖。不允许共享可变状态。
- 测试体内禁止条件判断：测试中出现 if/else、switch、循环说明要么被测代码需要重构，要么一个测试测了太多东西。
- 测试命名：`should_<预期行为>_when_<条件>`。例如 `should_return_overdue_books_when_user_has_past_due_items`。

## 测试类型
- 单元测试：隔离验证业务逻辑，外部依赖 mock。每个 ≤ 10ms。
- 集成测试：验证与真实外部系统（数据库、API、文件系统）的交互。使用 test container 或嵌入式实例。
- 禁止 flaky 测试：偶发失败的测试必须修复或移除。测试中禁止 sleep——使用带截止时间的轮询、事件等待或时间注入。
