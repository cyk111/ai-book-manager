---
description: 代码规范 - 函数、命名、错误处理、注释、数据不可变性
globs: **/*
alwaysApply: true
---

# 代码规范

## 函数与方法
- 单一职责：一个函数只做一件事，函数名描述它做什么。如果函数名需要"和"来连接，拆分它。
- 长度限制：每个函数/方法 ≤ 50 行。超过则提取私有辅助函数。
- 参数数量：≤ 4 个位置参数。超过则使用结构化参数对象。布尔参数是坏味道——考虑用两个函数或枚举代替。
- 早返回：使用卫语句（guard clause），嵌套 if/else 不超过 2 层。正常路径应靠左侧运行。
- 纯函数优先：计算型函数应该是纯函数（无副作用、无 I/O）。副作用（DB 写入、网络调用、文件 I/O）放在专门的 action 函数中，函数名必须体现其副作用（如 saveUser、sendNotification）。

## 错误处理
- 不允许静默吞异常。捕获后要么完全处理（重试、降级、恢复），要么附加上下文重新抛出。
- 每条错误消息必须包含足够上下文：哪个实体、什么操作、什么输入。例如 "User not found: id=abc123" 而非 "User not found"。
- 每个外部调用（HTTP、数据库、文件 I/O、消息队列）必须有超时设置。
- 区分可恢复错误（网络超时、临时锁→指数退避重试）和致命错误（schema 不匹配、认证失败→立即失败并给出清晰消息）。

## 命名
- 揭示意图：名称回答"做什么"或"用于什么"，而非"怎么实现"。calculateOverdueFee 而非 processData。
- 禁止缩写：getUserById 而非 getUsrById。例外：领域通用缩写（URL、JSON、HTML、ID）。
- 布尔变量：以 is、has、should、can 开头。如 isActive、hasPermission。

## 注释
- 解释"为什么"而非"是什么"：注释解释非显而易见的设计决策或业务逻辑原因，代码本身展示"做了什么"。
- 禁止僵尸注释：修改代码时同时更新或删除相关注释。与代码矛盾的注释比没有注释更糟。

## 数据与状态
- 优先不可变性：默认使用不可变数据结构。必须修改时，隔离到单一的、清晰命名的作用域内。
- 禁止魔法值：任何有业务含义的字面量必须定义为命名常量。
- 边界校验：所有外部输入在系统边界（API handler、CLI parser、消息消费者）校验。内部代码只操作已验证的数据。
